//sum of n even no
#include <stdio.h>

int main() {
    int n, sum = 0;

    printf("Enter the number of even numbers you want to sum: ");
    scanf("%d", &n);


sum=n*(n+1);
    printf("Sum of the first %d even numbers is: %d\n", n, sum);

    return 0; 
}
//done
